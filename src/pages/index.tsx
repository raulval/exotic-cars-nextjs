import type { NextPage } from "next";
import { HomeContainer } from "../styles/home";

const Home: NextPage = () => {
  return (
    <HomeContainer>
      <h1>Home Page</h1>
    </HomeContainer>
  );
};

export default Home;
